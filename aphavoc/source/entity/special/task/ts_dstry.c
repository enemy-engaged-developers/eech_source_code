// 
// 	 Enemy Engaged RAH-66 Comanche Versus KA-52 Hokum
// 	 Copyright (C) 2000 Empire Interactive (Europe) Ltd,
// 	 677 High Road, North Finchley, London N12 0DA
// 
// 	 Please see the document LICENSE.TXT for the full licence agreement
// 
// 2. LICENCE
//  2.1 	
//  	Subject to the provisions of this Agreement we now grant to you the 
//  	following rights in respect of the Source Code:
//   2.1.1 
//   	the non-exclusive right to Exploit  the Source Code and Executable 
//   	Code on any medium; and 
//   2.1.2 
//   	the non-exclusive right to create and distribute Derivative Works.
//  2.2 	
//  	Subject to the provisions of this Agreement we now grant you the
// 	following rights in respect of the Object Code:
//   2.2.1 
// 	the non-exclusive right to Exploit the Object Code on the same
// 	terms and conditions set out in clause 3, provided that any
// 	distribution is done so on the terms of this Agreement and is
// 	accompanied by the Source Code and Executable Code (as
// 	applicable).
// 
// 3. GENERAL OBLIGATIONS
//  3.1 
//  	In consideration of the licence granted in clause 2.1 you now agree:
//   3.1.1 
// 	that when you distribute the Source Code or Executable Code or
// 	any Derivative Works to Recipients you will also include the
// 	terms of this Agreement;
//   3.1.2 
// 	that when you make the Source Code, Executable Code or any
// 	Derivative Works ("Materials") available to download, you will
// 	ensure that Recipients must accept the terms of this Agreement
// 	before being allowed to download such Materials;
//   3.1.3 
// 	that by Exploiting the Source Code or Executable Code you may
// 	not impose any further restrictions on a Recipient's subsequent
// 	Exploitation of the Source Code or Executable Code other than
// 	those contained in the terms and conditions of this Agreement;
//   3.1.4 
// 	not (and not to allow any third party) to profit or make any
// 	charge for the Source Code, or Executable Code, any
// 	Exploitation of the Source Code or Executable Code, or for any
// 	Derivative Works;
//   3.1.5 
// 	not to place any restrictions on the operability of the Source 
// 	Code;
//   3.1.6 
// 	to attach prominent notices to any Derivative Works stating
// 	that you have changed the Source Code or Executable Code and to
// 	include the details anddate of such change; and
//   3.1.7 
//   	not to Exploit the Source Code or Executable Code otherwise than
// 	as expressly permitted by  this Agreement.
// 





#include "project.h"



#define DEBUG_MODULE 0



static void destroy_local (entity *en)
{

	task_kill_loss_data
		*destroy_kill = nullptr,
		*kill = nullptr;

	task
		*raw = nullptr;

	////////////////////////////////////////
	//
	// PRE-AMBLE
	//
	////////////////////////////////////////

	#if DEBUG_MODULE

	debug_log_entity_args (ENTITY_DEBUG_LOCAL, ENTITY_DEBUG_DESTROY, en);

	#endif

	raw = (task *) get_local_entity_data (en);

	if (raw->player_task)
	{
		debug_log ("TS_DSTRY : destroying player task %d", get_local_entity_safe_index (en));
	}

	////////////////////////////////////////
	//
	// VALIDATE
	//
	////////////////////////////////////////

	ASSERT (!raw->guide_root.first_child);

	ASSERT (!raw->waypoint_root.first_child);

	////////////////////////////////////////
	//
	// DESTROY COMPONENTS
	//
	////////////////////////////////////////

	////////////////////////////////////////
	//
	// UNLINK FROM SYSTEM
	//
	////////////////////////////////////////

	// leave on task lists for destroy

	unlink_local_entity_children (en, LIST_TYPE_PLAYER_TASK);

	unlink_local_entity_children (en, LIST_TYPE_TASK_DEPENDENT);

	delete_local_entity_from_parents_child_list (en, get_local_task_list_type (en));

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_PILOT_LOCK);

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_SECTOR_TASK);

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_TASK_DEPENDENT);

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_UPDATE);

	//
	// notify campaign screen (should be done AFTER unlinked from keysite)
	//

	if (task_database [raw->sub_type].primary_task)
	{
		notify_campaign_screen (CAMPAIGN_SCREEN_MISSION_DESTROYED, en);
	}

	////////////////////////////////////////
	//
	// FREE ENTITY DATA
	//
	////////////////////////////////////////

	if (raw->route_dependents)
	{

		free_mem (raw->route_dependents);
	}

	if (raw->route_formation_types)
	{

		free_mem (raw->route_formation_types);
	}

	if (raw->route_waypoint_types)
	{

		free_mem (raw->route_waypoint_types);
	}

	if (raw->route_nodes)
	{

		free_mem (raw->route_nodes);
	}

	// kills

	kill = raw->task_kills;

	while (kill)
	{

		destroy_kill = kill;

		kill = kill->next;

		free_mem (destroy_kill);
	}

	// losses

	kill = raw->task_losses;

	while (kill)
	{

		destroy_kill = kill;

		kill = kill->next;

		free_mem (destroy_kill);
	}

	free_mem (raw);

	set_free_entity (en);
}



static void destroy_remote (entity *en)
{
	#if DEBUG_MODULE

	debug_log_entity_args (ENTITY_DEBUG_REMOTE, ENTITY_DEBUG_DESTROY, en);

	#endif

	transmit_entity_comms_message (ENTITY_COMMS_DESTROY, en);
}



static void destroy_server (entity *en)
{
	//
	// destroy remote entity first (keeping local entity valid)
	//

	validate_client_server_remote_fn ();

	destroy_remote (en);

	validate_client_server_local_fn ();

	//
	// destroy local using 'full' function
	//

	destroy_local_entity (en);
}



static void destroy_client (entity *en)
{
	if (get_comms_data_flow () == COMMS_DATA_FLOW_TX)
	{
		validate_client_server_remote_fn ();

		destroy_remote (en);
	}
	else
	{
		validate_client_server_local_fn ();

		//
		// destroy local using 'full' function
		//

		destroy_local_entity (en);
	}
}



static void destroy_local_family (entity *en)
{

	destroy_local_entity_children (en, LIST_TYPE_GUIDE);

	destroy_local_entity_children (en, LIST_TYPE_WAYPOINT);

	destroy_local_entity (en);
}



static void destroy_server_family (entity *en)
{

	destroy_client_server_entity_children (en, LIST_TYPE_GUIDE);

	destroy_client_server_entity_children (en, LIST_TYPE_WAYPOINT);

	destroy_client_server_entity (en);
}



static void destroy_client_family (entity *en)
{
	#if DEBUG_MODULE

	debug_log_entity_args (ENTITY_DEBUG_REMOTE, ENTITY_DEBUG_DESTROY_FAMILY, en);

	#endif

	transmit_entity_comms_message (ENTITY_COMMS_DESTROY_FAMILY, en);
}



static void kill_local (entity *en)
{
	task
		*raw = nullptr;

	////////////////////////////////////////
	//
	// PRE-AMBLE
	//
	////////////////////////////////////////

	#if DEBUG_MODULE

	debug_log_entity_args (ENTITY_DEBUG_LOCAL, ENTITY_DEBUG_KILL, en);

	#endif

	raw = (task *) get_local_entity_data (en);

	ASSERT (get_local_task_list_type (en) != LIST_TYPE_UNASSIGNED_TASK);

	ASSERT (raw->task_state != TASK_STATE_UNASSIGNED);

	ASSERT (task_database [raw->sub_type].primary_task);
	
	ASSERT (task_database [raw->sub_type].visible_task);
	
	// task_dependent_root

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_PILOT_LOCK);

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_SECTOR_TASK);

	delete_local_entity_from_parents_child_list (en, LIST_TYPE_TASK_DEPENDENT);

	destroy_local_entity_children (en, LIST_TYPE_GUIDE);

	//
	// Set EXPIRE timer so the the task will be DESTROYED after a certain time
	//

	set_local_entity_float_value (en, FLOAT_TYPE_EXPIRE_TIMER, get_completed_task_expire_time ());

	debug_assert (raw->expire_timer > 0.0);
}



static void kill_remote (entity *en)
{
	#if DEBUG_MODULE

	debug_log_entity_args (ENTITY_DEBUG_REMOTE, ENTITY_DEBUG_KILL, en);

	#endif

	transmit_entity_comms_message (ENTITY_COMMS_KILL, en);
}



static void kill_server (entity *en)
{
	//
	// kill remote entity first (keeping local entity valid)
	//

	validate_client_server_remote_fn ();

	kill_remote (en);

	validate_client_server_local_fn ();

	//
	// kill local using 'full' function
	//

	kill_local_entity (en);
}



static void kill_client (entity *en)
{
	if (get_comms_data_flow () == COMMS_DATA_FLOW_TX)
	{
		validate_client_server_remote_fn ();

		kill_remote (en);
	}
	else
	{
		validate_client_server_local_fn ();

		//
		// kill local using 'full' function
		//

		kill_local_entity (en);
	}
}



void overload_task_destroy_functions (void)
{
	fn_destroy_local_entity						[ENTITY_TYPE_TASK]							= destroy_local;
	fn_destroy_client_server_entity			[ENTITY_TYPE_TASK][COMMS_MODEL_SERVER]	= destroy_server;
	fn_destroy_client_server_entity			[ENTITY_TYPE_TASK][COMMS_MODEL_CLIENT]	= destroy_client;

	fn_destroy_local_entity_family			[ENTITY_TYPE_TASK]							= destroy_local_family;
	fn_destroy_client_server_entity_family	[ENTITY_TYPE_TASK][COMMS_MODEL_SERVER]	= destroy_server_family;
	fn_destroy_client_server_entity_family	[ENTITY_TYPE_TASK][COMMS_MODEL_CLIENT]	= destroy_client_family;

	fn_kill_local_entity							[ENTITY_TYPE_TASK]							= kill_local;
	fn_kill_client_server_entity				[ENTITY_TYPE_TASK][COMMS_MODEL_SERVER]	= kill_server;
	fn_kill_client_server_entity				[ENTITY_TYPE_TASK][COMMS_MODEL_CLIENT]	= kill_client;
}


